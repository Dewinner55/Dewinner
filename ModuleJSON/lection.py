# JSON - JavaScript Oject Natation
# Едины формат, хранения и передачи данных мжду приложениями, сайтами, сервисами и языка программирование через сеть интернет
# <filemaname>.json файл в формате JSON

# {
    # "id": 1,
    # "author": "Ed Shreeran",
    # "title": "Perfect",
    # "year": 2015,
    # "fit": true,
    # "feat": null
# } --- Это JSON
# наша задача научиться переводить данные JSON в Python dictionary
# !!!
# JS object == {key: value}
# PY dict == {key: value}
# JSON == {key: value}

# Процессы Сериализации и Десериализации данных
# Сериализация (Запись данных в JSON) - Это перевод из Python объектов в JSON формат
# json.dump -> метод записывает Python данные в ФАЙЛ в формате JSON, паралельно сделав Сериализацию
# json.dumps -> метод записывает Python данные в СТРОКУ в формате JSON, паралельно сделав Сериализацию

# Десериализцая (это чтение данных из JSON) -> это процесс перевода из JSON в Python dictionary
# json.load -> метод который считывает ФАЙЛ в формате JSON и переводит его в Python object
# json.loads -> метод который считывает ТЕКСТ в формате JSON и переводит его в Python object






